

define(['enums/OperationType'], function(operationType) {
       'use strict';
       
       describe("calculator operations", function () {
                it("calculates '6 + 2'", function () {
                   perationType.getResult(1,2, operationType.SUM).toBe(3);
                   });

                });
       });